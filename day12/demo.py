"""Реплики сценариев: для кнопок в браузере и для замеров в scenarios.py.

Один список на оба места: кнопка печатает то же самое, что считает замер, и таблицы
в README остаются про тот же разговор, который видно на экране.
"""

# Три вопроса, которые задаются всем профилям сразу. Вопрос один и тот же, память
# одна и та же — отличается только профиль, и в ответах видно ровно его.
#
# Вопросы выбраны так, чтобы шкалы разошлись: первый провоцирует сравнение (таблица
# против прозы), второй — объяснение термина (эксперт против новичка), третий —
# рассуждение, где длину ответа не задаёт ничего, кроме профиля.
COMPARE = (
    "Очередь для асинхронных выгрузок: остаться на Celery или взять RQ?",
    "Что такое подписанная ссылка и зачем она нужна для файлов выгрузки?",
    "Хотим отдавать отчёт по остаткам за любой день. Что тут может пойти не так?",
)

# Разговор, в котором профиль меняется на ходу. Ходы 1–2 идут с исходным профилем,
# на ходах 3–4 пользователь просит другую форму ответа — и это просьбы не про
# задачу, а про то, как с ним говорить: они уходят в профиль правкой шкал. Ходы 5–7
# показывают, что дальше агент держит новую форму сам, без напоминаний.
#
# Прогон идёт от лица стажёра: у него в профиле «подробно» и «списки», поэтому обе
# просьбы — настоящая правка, а разницу в ответах видно без линейки. Профиль назван
# здесь, а не в замере: кнопка автопрогона в браузере переключается на него же,
# иначе на экране было бы видно не то, что в таблицах README.
LESSON_PROFILE = "kostya"

DIALOG = (
    "Нужен сервис выгрузки складских остатков: оператор скачивает отчёт "
    "за любой день. С чего начать?",
    "Форматы отчёта — CSV и XLSX, больше никаких не нужно.",
    "Давай короче: мне хватает двух-трёх предложений.",
    "И без списков, пиши обычным текстом.",
    "Готовые файлы храним семь дней, потом удаляем.",
    "Доступ к выгрузке только у роли warehouse_manager, остальным 403.",
    # Ход про общее правило оставлен нарочно: в одном разговоре видно все три
    # адреса — правка профиля, требование задачи и решение, верное вне её.
    "Кстати, асинхронные выгрузки мы теперь делаем во всех сервисах — это общее правило.",
)

# Ходы, на которых пользователь просит другую форму ответа. Нужны замеру, чтобы
# развести «до» и «после» в таблице, и README, чтобы не считать их вручную.
LESSON_TURNS = (3, 4)

# Контрольный вопрос после прогона. Он про задачу, а не про форму, и задаётся в
# новом диалоге того же профиля: правка предпочтения не должна остаться внутри того
# разговора, где её сделали.
LESSON_CHECK = "Что нам ещё стоит решить по выгрузке остатков?"
