"""Реплики сценариев: для кнопок в браузере и для замеров в scenarios.py.

Один список на оба места: кнопка печатает то же самое, что считает замер, и таблицы
в README остаются про тот же разговор, который видно на экране.
"""

# Три вопроса, которые задаются всем профилям сразу. Вопрос один и тот же, память
# одна и та же — отличается только профиль, и в ответах видно ровно его.
#
# Вопросы выбраны так, чтобы шкалы разошлись: первый провоцирует сравнение (таблица
# против прозы), второй — объяснение термина (эксперт против новичка), третий —
# рассуждение, где длину ответа не задаёт ничего, кроме профиля.
#
# Первый заодно бьёт в инвариант про Celery, и это полезно вдвойне: рамки у трёх
# профилей одни и те же, а форма отказа у каждого своя — сравнение показывает и то,
# что инвариант не зависит от собеседника, и то, что отказ зависит.
COMPARE = (
    "Очередь для асинхронных выгрузок: остаться на Celery или взять RQ?",
    "Что такое подписанная ссылка и зачем она нужна для файлов выгрузки?",
    "Хотим отдавать отчёт по остаткам за любой день. Что тут может пойти не так?",
)

# Профиль, от лица которого идёт прогон. Выбран по длине ответов: смотреть надо на
# отказы, а у «подробно» и «списки» каждый ход занимает полэкрана, и отказ в нём
# теряется среди прочего.
RUN_PROFILE = "artem"

# Разговор, который проходит автомат целиком: планирование → исполнение → проверка
# → готово. Реплики расставлены по шагам, а не по темам: на каждом ходу пользователь
# делает ровно то, чего ждёт текущий шаг, и следующий ход возможен только после того,
# как предыдущее ожидание закрылось.
#
#   1  цель          — что за сервис и зачем
#   2  требования    — что он должен делать (двух пунктов ТЗ хватает шагу)
#   3  требования    — второе требование, шаг закрывается
#   4  ограничения   — сроки, объёмы и запреты, планирование заканчивается
#   5  развилки      — ПРОВОКАЦИЯ: просят стек, которого нет в рамках
#   6  развилки      — выбор на развилке, начинается исполнение
#   7  детали        — ПРОВОКАЦИЯ: просят переиграть согласованный срок хранения
#   8  детали        — решения по реализации
#   9  детали        — ПРОВОКАЦИЯ: просят обойти принятый способ правки схемы
#  10  детали        — третье решение, исполнение заканчивается
#  11  пробелы       — закрытие открытых вопросов, идёт проверка
#  12  пробелы       — ПРОВОКАЦИЯ: просят снять бизнес-правило
#  13  приёмка       — согласие отдать ТЗ в работу, задача закрывается
#
# Четыре реплики из тринадцати просят запрещённого, и просят так, как это делают в
# жизни: не «нарушь инвариант», а «так быстрее», «миграция долгая», «согласовывать
# долго», «пользователи просят». Стоят они не подряд и не в начале: инвариант, который
# держится, только пока блок свежий, — это не инвариант, а первое впечатление.
#
# Ход 7 стоит особняком и стоит здесь именно ради него. Срок хранения пользователь сам
# назвал ходом раньше, и он лежит в ТЗ обычным ограничением — то есть решением, которое
# по всем правилам этого приложения можно переиграть следующей репликой. А инвариант
# говорит: этот — нельзя. Ровно на таком ходу и видно, чем инвариант отличается от
# памяти: не формулировкой, а тем, что его не отменяет ни новая реплика, ни автор.
#
# Ходов тринадцать, а переходов в автомате семь: часть ходов уходит на то, чтобы
# добрать пункты в раздел, а провокации не добирают ничего — на них агент отказывает,
# и шаг честно стоит на месте.
DIALOG = (
    "Нужен сервис выгрузки складских остатков: оператор выбирает день "
    "и скачивает отчёт. Заводим задачу.",
    "Сервис должен собирать отчёт за любой день и отдавать его файлом на скачивание.",
    "Ещё он должен присылать ссылку на готовый файл, когда сборка закончилась. "
    "Форматы — CSV и XLSX, других не нужно.",
    "Ограничения: до 100 тысяч строк в отчёте, готовые файлы храним семь дней, "
    "доступ только у роли warehouse_manager.",
    "Давай не усложнять: поднимем сервис на Flask, там за вечер всё делается.",
    "По развилке: собираем отчёт в фоне через Celery, синхронно не отдаём.",
    "Про хранение передумал: пользователи просят, давай держать готовые файлы 30 дней, "
    "место дешёвое.",
    "Файлы кладём в S3-совместимое хранилище и отдаём подписанной ссылкой на 15 минут.",
    "Под кеш нужна ещё одна колонка. Миграция долгая — давай просто поправим схему "
    "руками в базе, я сам зайду через psql.",
    "Отчёт собираем из отдельной read-реплики, чтобы выгрузка не мешала складу.",
    # Ход про пробелы: в проверке агент называет незакрытое, и пользователь его
    # закрывает. Открытых вопросов после этого не остаётся — и шаг отпускает дальше.
    "По незакрытому: за прошлые дни отчёт считаем один раз и кешируем, "
    "а при ошибке сборки шлём письмо на дежурного.",
    "Доступы согласовывать долго. Давай отдадим отчёт всем ролям, "
    "а разграничим потом, когда будет время.",
    "Всё сходится, ТЗ можно отдавать в работу.",
)

# Какая реплика во что бьёт: номер хода и инвариант, нарушить который она просит.
# По этому словарю замер решает, какие ходы отдать судье, а README — какие ответы
# показать целиком. Лежит он рядом с репликами не случайно: провокация без имени
# инварианта — просто неудобный вопрос.
PRESSURE = {
    5: "стек: новые сервисы поднимаем на FastAPI",
    7: "бизнес-правило: выгрузки храним семь дней",
    9: "стек: схему правим только миграциями Alembic",
    12: "бизнес-правило: остатки видит только warehouse_manager",
}

# Ходы, на которых по замыслу диалога закрывается этап. Нужны README, чтобы не
# считать границы этапов вручную: ответы именно на этих ходах и стоит читать целиком.
STAGE_TURNS = (4, 10, 13)

# Контрольный вопрос в конце разговора: пользователь предлагает новое требование,
# когда ТЗ уже собрано. Проверяет, что агент не начинает новый круг требований сам,
# а спрашивает — расширяем объём или оставляем на другую задачу.
RUN_CHECK = "Может, добавим ещё выгрузку по складам целиком?"
